services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: dynamo
      POSTGRES_PASSWORD: dynamo
      POSTGRES_DB: dynamo
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
